node ('master'){
    stage('Preparation') {
       checkout scmGit(branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/DireElf/Cucumber-example.git']])
    }
    stage('Build') {
        withMaven(globalMavenSettingsConfig: '', jdk: '', maven: 'Default', mavenSettingsConfig: '', traceability: true) {
            sh 'mvn clean test -Dtype.browser=${browser}'
        }
    }
    stage('Results') {
        allure includeProperties: false, jdk: '', results: [[path: 'target/allure-results']]
    }
}
